{
  "name": "@vercom/common",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./types/*": "./dist/types/*.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
